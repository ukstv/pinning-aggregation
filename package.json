{
  "name": "@pinning-aggregation/mono",
  "private": true,
  "version": "1.0.0",
  "description": "Use multiple pinning services like they are one",
  "scripts": {
    "test": "jest",
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm run build --recursive",
    "clean": "pnpm run clean --recursive"
  },
  "author": "Sergey Ukustov <sergey@ukstv.me>",
  "license": "(MIT OR Apache-2.0)",
  "engines": {
    "node": ">=12",
    "pnpm": ">=5.4"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "jest": "^26.6.1",
    "prettier": "^2.1.2",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.4.3",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.5"
  }
}
